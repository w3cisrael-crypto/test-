# התחלה מהירה - למתחילים מוחלטים 🚀

**אתה מתחיל בתכנות? מדריך זה בשבילך!**

## אפשרות 1: הרצה באינטרנט (ללא התקנה!) 🌐

**זה הדרך הכי קלה! לא צריך להתקין כלום על המחשב.**

### מה צריך?
1. חשבון GitHub (חינמי)
2. חשבון Google Cloud (חינמי)
3. 10-15 דקות

### המדריך המלא
ראה את הקובץ **DEPLOY.md** - יש שם מדריך צעד אחר צעד עם תמונות והסברים.

**בקצרה:**
1. העלה את הקוד ל-GitHub
2. צור Service Account ב-Google Cloud
3. פתח חשבון ב-Streamlit Cloud (חינמי)
4. העלה את האפליקציה
5. זהו! המערכת רצה באינטרנט

**קישור למדריך מלא:** [DEPLOY.md](DEPLOY.md)

---

## אפשרות 2: הרצה על המחשב שלך 💻

### דרישות מקדימות

**1. התקן Python** (אם עדיין לא התקנת):

- **Windows**:
  1. גש ל-https://www.python.org/downloads/
  2. הורד Python 3.9 ומעלה
  3. **חשוב!** סמן את "Add Python to PATH" בהתקנה

- **Mac**:
  ```bash
  brew install python3
  ```

- **Linux (Ubuntu/Debian)**:
  ```bash
  sudo apt update
  sudo apt install python3 python3-pip python3-venv
  ```

**2. קובץ Service Account מ-Google**:
- עקוב אחר שלב 2 ב-DEPLOY.md
- שמור את הקובץ בשם `service-account.json`

### התקנה מהירה (3 דקות)

#### Windows:
1. פתח **Command Prompt** או **PowerShell**
2. נווט לתיקיית הפרויקט:
   ```cmd
   cd C:\path\to\test-
   ```
3. הרץ את סקריפט ההתקנה:
   ```cmd
   setup.bat
   ```
4. זהו! המערכת מותקנת

#### Mac/Linux:
1. פתח **Terminal**
2. נווט לתיקיית הפרויקט:
   ```bash
   cd /path/to/test-
   ```
3. הרץ את סקריפט ההתקנה:
   ```bash
   ./setup.sh
   ```
4. זהו! המערכת מותקנת

### הרצת המערכת

אחרי ההתקנה, הרץ:

```bash
streamlit run app.py
```

הדפדפן יפתח אוטומטית ב-`http://localhost:8501`

**לא עובד?** ראה "פתרון בעיות" למטה.

---

## איך להשתמש במערכת? 📖

### ממשק המערכת

1. **סרגל צד (שמאל)**:
   - הזן כתובת אתר (למשל: `https://example.com`)
   - בחר תקופת נתונים (90 ימים מומלץ)
   - הוסף מילות מותג (אופציונלי)
   - לחץ "טען נתונים"

2. **4 טאבים במרכז**:
   - **📊 סקירה**: מבט על כללי - גרפים ומספרים
   - **🎯 הזדמנויות**: רשימת מילות מפתח עם המלצות
   - **🔗 קלאסטרים**: קיבוצי תוכן למניעת קניבליזציה
   - **📅 תוכנית**: יצירת תוכנית תוכן חודשית

### דוגמת שימוש

1. הזן: `https://yoursite.com`
2. בחר: 90 ימים
3. לחץ "טען נתונים" ← **המתן 10-30 שניות**
4. עבור לטאב "הזדמנויות" ← **ראה המלצות**
5. עבור לטאב "תוכנית" ← **צור תוכנית תוכן**
6. הורד את התוכנית ← **שתף עם הצוות**

---

## פתרון בעיות נפוצות 🔧

### "Python is not recognized"
**פתרון (Windows)**:
1. התקן Python מחדש
2. ודא שסימנת "Add Python to PATH"
3. פתח Command Prompt חדש

### "streamlit: command not found"
**פתרון**:
```bash
pip install streamlit
```

### "Service account credentials not found"
**פתרון**:
1. ודא שקובץ `service-account.json` נמצא בתיקיית הפרויקט
2. ודא שהשם מדויק (אותיות קטנות, מקף במקום רווח)

### "Permission denied"
**פתרון**:
1. ודא שהוספת את Service Account ל-Search Console
2. המייל צריך להיות: `something@something.iam.gserviceaccount.com`
3. תן הרשאות "Full"

### האפליקציה לא פותחת בדפדפן
**פתרון**:
1. פתח ידנית: `http://localhost:8501`
2. או בדוק אם פורט 8501 תפוס:
   ```bash
   # Windows
   netstat -ano | findstr :8501

   # Mac/Linux
   lsof -i :8501
   ```

---

## שאלות נפוצות ❓

**Q: האפליקציה עובדת רק על המחשב שלי?**
A: כן, אלא אם תעלה אותה לאינטרנט (ראה DEPLOY.md)

**Q: האם אני צריך לשלם משהו?**
A: לא! הכל חינמי:
- Python - חינמי
- Streamlit - חינמי
- Streamlit Cloud - חינמי (עד 3 אפליקציות)
- Google Search Console API - חינמי

**Q: כמה זמן לוקח לטעון נתונים?**
A: בין 10 שניות ל-2 דקות, תלוי בכמות הנתונים

**Q: אפשר להריץ מספר אתרים?**
A: כן! פשוט הזן אתר אחר בסרגל הצד

**Q: איך משתפים את התוכניות עם הצוות?**
A: לחץ על כפתור "הורד" בטאב "תוכנית" - תקבל קובץ CSV/JSON

**Q: המערכת לא עובדת!**
A:
1. בדוק את הלוגים (הטקסט האדום)
2. ראה "פתרון בעיות" למעלה
3. פתח Issue ב-GitHub

---

## מה הלאה? 🎯

**למדת איך להריץ? מעולה!**

עכשיו:
1. **נסה עם האתר שלך** - הזן את הכתובת וראה תוצאות
2. **שחק עם ההגדרות** - נסה אסטרטגיות שונות
3. **הורד תוכניות** - שתף עם צוות התוכן
4. **העלה לאינטרנט** - ראה DEPLOY.md

**רוצה ללמוד עוד על הקוד?**
- ראה את README.md למידע טכני
- בדוק את התיקייה `src/` לקוד המקור
- הרץ בדיקות: `pytest tests/`

---

**בהצלחה! אם יש שאלות, פתח Issue ב-GitHub 🚀**

---

## תזכורת מהירה

### הרצה מקומית
```bash
# 1. התקנה (פעם אחת)
./setup.sh  # Mac/Linux
setup.bat   # Windows

# 2. הרצה (כל פעם)
streamlit run app.py
```

### הרצה באינטרנט
👉 **ראה [DEPLOY.md](DEPLOY.md)** למדריך מלא
